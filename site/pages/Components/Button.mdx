import { Callout, Tab, Tabs, Steps } from 'nextra-theme-docs'

import { Button1, Button2, Button3, Button4,Button5, Button6,FaceBookButton,GithubButton,GoogleButton,MasterCardButton,PayPalButton,TwitterButton,} from 'love-simple-ui'

## Customizable Buttons

Buttons are html elements that can be used to trigger actions. They can be customized to fit your needs.
We provides a large variety of styles and sizes for the button component including outlined buttons, multiple colors, sizes, buttons with icons, and more.

<Steps>

### Installation
To use the buttons, you need to install the package first and complete the setup.
Setup instructions can be found [here](https://love-simple-ui.vercel.app/Installation).

### Import
Import the buttons in your project.

```js
import { ButtonComponent } from 'love-simple-ui'
```

### Enjoy using the buttons
You can now use the buttons in your project and customize them to fit your needs. 🎉
</Steps>

We have various types of buttons that can be used in your project. The types are listed below.

## Gradient Background Buttons

These buttons have a gradient background and a hover effect.

<Callout emoji="💡">
  You can use the <code>tailwindCSS</code> classes to customize the buttons.
</Callout>

### Button1
Button1 has sky blue and cyan gradient background button with a hover effect.

<Tabs items={['preview', 'code']}>
  <Tab>
    <Button1 className="hover:underline font-medium">
      Hello World
    </Button1>
  </Tab>
  <Tab>
    ```js filename="pages/index.js" {2,7,8,9} copy
    import React from 'react';
    import { Button1 } from "love-simple-ui";

    function App() {
        return (
            <div>
                <Button1 className="hover:underline font-medium">
                    Hello World
                </Button1>
            </div>
        )
    }

    export default App;
    ```
  </Tab>
</Tabs>

### Some More Buttons
To use these below buttons follow the same code and replace the button name with the button name you want to use. For example, if you want to use Button4, replace Button3 with Button4.

<div className="pt-4 flex items-center gap-3">
<Button2>
    Button 2
</Button2>
<Button3>
    Button 3
</Button3>
<Button4>
    Button 4
</Button4>
<Button5>
    Button 5
</Button5>
<Button6>
    Button 6
</Button6>
</div>

## Social Media Buttons

These buttons are used to link your social media accounts to your website and these are also customizable.
To use these buttons follow the same code and replace the button name with the button name you want to use. For example, if you want to use FaceBookButton, replace Button3 with FaceBookButton.

### FaceBookButton
FaceBookButton has a Facebook icon and a hover effect.

<Tabs items={['preview', 'code']}>
  <Tab>
    <FaceBookButton/>
  </Tab>
  <Tab>
    ```js filename="pages/index.js" {2,7,8,9} copy
    import React from 'react';
    import { FaceBookButton } from "love-simple-ui";

    function App() {
        return (
            <div>
               <FaceBookButton className=""/>
            </div>
        )
    }

    export default App;
    ```
  </Tab>
</Tabs>

### Some More Social Media Buttons
To use these below buttons follow the same code and replace the button name with the button name you want to use. For example, if you want to use GithubButton, replace FaceBookButton with GithubButton.

<div className="pt-4 flex items-center gap-3">
<FaceBookButton/>
<GithubButton/>
<GoogleButton/>
<TwitterButton/>
</div>

## Payment Buttons

These buttons are used to link your payment accounts to your website and these are also customizable. To use these buttons follow the same code and replace the button name with the button name you want to use. For example, if you want to use MasterCardButton, replace PayPalButton with MasterCardButton.

### PayPalButton
PayPalButton has a PayPal icon and a hover effect.

<Tabs items={['preview', 'code']}>
  <Tab>
    <PayPalButton/>
  </Tab>
  <Tab>
    ```js filename="pages/index.js" {2,7,8,9} copy
    import React from 'react';
    import { PayPalButton } from "love-simple-ui";

    function App() {
        return (
            <div>
               <PayPalButton className=""/>
            </div>
        )
    }

    export default App;
    ```
  </Tab>
</Tabs>

### Some More Payment Buttons
To use these below buttons follow the same code and replace the button name with the button name you want to use. For example, if you want to use MasterCardButton, replace PayPalButton with MasterCardButton.

<div className="pt-4 flex items-center gap-3">
<PayPalButton/>
<MasterCardButton/>
</div>