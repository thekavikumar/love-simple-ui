import React from 'react';
import InfiniteScroll from './infinitescroll';

export default {
  title: 'Components/InfiniteScroll',
  component: InfiniteScroll,
};

const Template = () => <InfiniteScroll />;

export const Default = Template.bind({});
